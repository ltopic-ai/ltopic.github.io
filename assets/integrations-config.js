/* Public configuration only. Never put email addresses, passwords or API secrets here.
   Enable each provider only after owner authentication, settings review and live verification. */
window.LtopicConfig = Object.freeze({
  ga4Id: '',
  clarityId: '',
  formEndpoint: '', // Activated opaque FormSubmit AJAX URL; never the recipient address.
  consentVersion: 1
});
